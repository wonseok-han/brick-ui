# 워크스페이스 패키지는 항상 심볼릭 링크로 연결
link-workspace-packages=true

# peer 불일치를 에러가 아닌 경고로. 라이브러리 개발 중 React 버전 범위가 넓어 필요.
strict-peer-dependencies=false

# 배포 패키지가 팬텀 의존성에 기대지 않도록 호이스팅 금지 (기본값이지만 명시)
shamefully-hoist=false
